# 마이페이지(My Page) v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Figma `702:7858` 마이페이지를 Next.js App Router 중첩 라우트로 구현하고, "내 계정 관리" 탭을 기존 백엔드 API와 end-to-end 연동한 뒤 나머지 3탭을 순차 추가한다.

**Architecture:** `src/app/mypage/`에 공통 `layout.tsx`(4탭 네비 + 로그인 가드)와 탭별 라우트(account/saved/ai-history/subscription)를 둔다. 데이터는 기존 `src/service/index.ts`의 `userService`/`myService`를 react-query 훅으로 래핑해 사용한다. 백엔드에 없는 기능(비밀번호·결제수단·OAuth·이메일 변경)은 화면단 graceful degrade.

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript 6 · @tanstack/react-query 5 · jotai (auth) · axios (shared `http`) · Tailwind CSS 4 · class-variance-authority · zod.

## Global Constraints

- **작업 레포/브랜치:** `JECT6--4th-Frontend`, 브랜치 `feat/mypage` (base `main`). PR도 이 레포로. 백엔드 코드 변경 금지.
- **테스트 러너 없음:** 이 레포엔 vitest/jest 없음(Playwright만 devDep). 각 태스크 자동 게이트는 `yarn type-check` → `yarn lint` 순서로 통과해야 하며, 마지막/연동 태스크는 `yarn build`와 로컬 백엔드 연동 수동확인까지 포함한다. 단위 테스트 러너를 새로 도입하지 않는다.
- **API 베이스:** 모든 호출은 기존 `http`(`src/shared/api/http.ts`) 경유. 토큰 주입·`{success,data}` 언래핑·snake/camel 변환은 자동. 서비스 레이어는 이미 존재하므로 신규 fetch 코드를 직접 작성하지 않는다.
- **인증:** 로그인 여부는 `isLoggedInAtom`(jotai, `@/entities/auth`). 미로그인 시 `/auth/login`으로 redirect.
- **스타일 토큰:** 기존 Tailwind 커스텀 토큰 사용 (`neutral_20`, `neutral_60`, `neutral_95`, `violet_80`, `max-w-300`, `text-16/leading-24`, `font-pretendard` 등). 신규 색/간격 임의 추가 금지 — Figma `get_design_context` 결과의 토큰을 우선 사용.
- **커밋 author:** `git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit ...` (JECT-Study 레포 푸시 identity). 커밋은 태스크 단위로 자주.
- **import 컨벤션:** 절대경로 alias `@/` 사용. 'use client'는 상호작용/훅 쓰는 컴포넌트 상단에 선언.

---

## File Structure

```
src/app/mypage/
├── layout.tsx                  # 공통 레이아웃: 4탭 네비 + 로그인 가드 (Task 1)
├── page.tsx                    # /mypage → /mypage/account 리다이렉트 (Task 1)
├── account/page.tsx            # 내 계정 관리 (Task 2~5)
├── saved/page.tsx              # 관심 공고 (Task 6)
├── ai-history/page.tsx         # AI 히스토리 (Task 7)
├── subscription/page.tsx       # 구독/결제 (Task 8)
├── _components/
│   ├── MyPageTabs.tsx          # 탭 네비게이션 (Task 1)
│   ├── ProfileHeader.tsx       # 닉네임 + 편집 (Task 2~4)
│   ├── BlogLinkSection.tsx     # 블로그 연동 + 편집 (Task 2~4)
│   ├── CategorySection.tsx     # 관심 카테고리 + 편집 (Task 2~4)
│   ├── AccountNavCard.tsx      # 하단 진입 카드 (Task 2,5)
│   └── SectionShell.tsx        # 섹션 제목+컨텐츠 공통 래퍼 (Task 2)
└── hooks/
    ├── useMyProfile.ts         # getMe 조회 (Task 3)
    └── useProfileMutations.ts  # updateMe / linkBlog (Task 4)
```

재사용(신규 작성 X): `@/service`(userService/myService), `@/entities/{user,my,auth}`, `@/components/ui/button`, `@/shared/ui/input/Input`, `@/lib/utils`(cn).

---

### Task 1: 마이페이지 골격 (라우트 + 탭 네비 + 로그인 가드)

**Files:**
- Create: `src/app/mypage/layout.tsx`
- Create: `src/app/mypage/page.tsx`
- Create: `src/app/mypage/_components/MyPageTabs.tsx`
- Create: `src/app/mypage/account/page.tsx` (stub)
- Create: `src/app/mypage/saved/page.tsx` (stub)
- Create: `src/app/mypage/ai-history/page.tsx` (stub)
- Create: `src/app/mypage/subscription/page.tsx` (stub)

**Interfaces:**
- Produces: route group `/mypage/*`; `MyPageTabs` 컴포넌트(props 없음, `usePathname`로 활성 탭 판별). 각 탭 page는 default-export React 컴포넌트.

- [ ] **Step 1: 탭 네비 컴포넌트 작성**

`src/app/mypage/_components/MyPageTabs.tsx`:
```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'

import { cn } from '@/lib/utils'

const TABS = [
  { href: '/mypage/account', label: '내 계정 관리' },
  { href: '/mypage/saved', label: '관심 공고' },
  { href: '/mypage/ai-history', label: 'AI 히스토리' },
  { href: '/mypage/subscription', label: '구독/결제' },
] as const

export function MyPageTabs() {
  const pathname = usePathname()

  return (
    <nav className="flex gap-0 border-b border-neutral_95">
      {TABS.map(tab => {
        const active = pathname === tab.href
        return (
          <Link
            key={tab.href}
            href={tab.href}
            className={cn(
              'relative px-4 pt-6 pb-4 text-18 font-semibold leading-9 transition-colors',
              active ? 'text-neutral_20' : 'text-neutral_60 hover:text-neutral_30'
            )}
          >
            {tab.label}
            {active && (
              <span className="absolute inset-x-0 bottom-0 h-[3px] rounded-full bg-neutral_20" />
            )}
          </Link>
        )
      })}
    </nav>
  )
}
```

- [ ] **Step 2: 공통 레이아웃 + 로그인 가드 작성**

`src/app/mypage/layout.tsx`:
```tsx
'use client'

import { useAtomValue } from 'jotai'
import { useRouter } from 'next/navigation'
import { type ReactNode, useEffect } from 'react'

import { isLoggedInAtom } from '@/entities/auth'

import { MyPageTabs } from './_components/MyPageTabs'

export default function MyPageLayout({ children }: { children: ReactNode }) {
  const isLoggedIn = useAtomValue(isLoggedInAtom)
  const router = useRouter()

  useEffect(() => {
    if (!isLoggedIn) {
      router.replace('/auth/login')
    }
  }, [isLoggedIn, router])

  if (!isLoggedIn) {
    return null
  }

  return (
    <main className="mx-auto w-full max-w-300 px-5 pb-30">
      <MyPageTabs />
      {children}
    </main>
  )
}
```

- [ ] **Step 3: `/mypage` 리다이렉트 + 4개 탭 stub 작성**

`src/app/mypage/page.tsx`:
```tsx
import { redirect } from 'next/navigation'

export default function MyPage() {
  redirect('/mypage/account')
}
```

`src/app/mypage/account/page.tsx`:
```tsx
export default function AccountPage() {
  return <section className="pt-8">내 계정 관리</section>
}
```

`src/app/mypage/saved/page.tsx`:
```tsx
export default function SavedPage() {
  return <section className="pt-8">관심 공고</section>
}
```

`src/app/mypage/ai-history/page.tsx`:
```tsx
export default function AiHistoryPage() {
  return <section className="pt-8">AI 히스토리</section>
}
```

`src/app/mypage/subscription/page.tsx`:
```tsx
export default function SubscriptionPage() {
  return <section className="pt-8">구독/결제</section>
}
```

- [ ] **Step 4: 게이트 — 타입체크 & 린트**

Run: `yarn type-check && yarn lint`
Expected: 에러 0. (실패 시 import 경로/타입 수정)

- [ ] **Step 5: 런타임 확인 (수동)**

`yarn dev` 실행 후:
- 미로그인 상태에서 `http://localhost:3000/mypage` 접근 → `/auth/login`으로 리다이렉트되는지 확인.
- 로그인 상태(localStorage `auth`에 accessToken 존재)에서 `/mypage` → `/mypage/account`로 이동하고 4탭이 보이며 활성 탭 언더라인이 동작하는지 확인.

- [ ] **Step 6: 커밋**

```bash
git add src/app/mypage
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -m "feat(mypage): 라우트 골격 + 탭 네비 + 로그인 가드"
```

---

### Task 2: "내 계정 관리" 탭 정적 UI (연동 전)

**Files:**
- Create: `src/app/mypage/_components/SectionShell.tsx`
- Create: `src/app/mypage/_components/ProfileHeader.tsx`
- Create: `src/app/mypage/_components/BlogLinkSection.tsx`
- Create: `src/app/mypage/_components/CategorySection.tsx`
- Create: `src/app/mypage/_components/AccountNavCard.tsx`
- Modify: `src/app/mypage/account/page.tsx`

**Interfaces:**
- Produces:
  - `SectionShell({ title, action, children }: { title: string; action?: ReactNode; children: ReactNode })`
  - `ProfileHeader({ nickname, onEdit }: { nickname: string; onEdit?: () => void })`
  - `BlogLinkSection({ blogUrl, onEdit }: { blogUrl: string | null; onEdit?: () => void })`
  - `CategorySection({ categories, onEdit }: { categories: string[]; onEdit?: () => void })` — `categories`는 한글 라벨 문자열 배열
  - `AccountNavCard({ title, description, onClick, disabled, badge }: { title: string; description: string; onClick?: () => void; disabled?: boolean; badge?: string })`

- [ ] **Step 1: Figma 디자인 토큰 추출**

`get_design_context`(Figma MCP) 호출: fileKey `dwJ8bn41tF7w2qiqAUymf8`, nodeId `702:7858`. 반환된 spacing/typography/color 토큰을 아래 컴포넌트의 Tailwind 클래스에 반영(Global Constraints의 토큰 우선). 본 플랜의 클래스는 구조 기준값이며 Figma 결과로 정밀화한다.

- [ ] **Step 2: SectionShell 작성**

`src/app/mypage/_components/SectionShell.tsx`:
```tsx
import type { ReactNode } from 'react'

export function SectionShell({
  title,
  action,
  children,
}: {
  title: string
  action?: ReactNode
  children: ReactNode
}) {
  return (
    <section className="mt-14">
      <div className="flex items-center justify-between">
        <h2 className="text-20 font-semibold leading-8 text-neutral_20">{title}</h2>
        {action}
      </div>
      <div className="mt-6">{children}</div>
    </section>
  )
}
```

- [ ] **Step 3: ProfileHeader / BlogLinkSection / CategorySection / AccountNavCard 작성**

`src/app/mypage/_components/ProfileHeader.tsx`:
```tsx
export function ProfileHeader({ nickname, onEdit }: { nickname: string; onEdit?: () => void }) {
  return (
    <div className="flex items-end gap-4 pt-12">
      <h1 className="text-40 font-bold leading-12 text-neutral_20">{nickname}</h1>
      <button
        type="button"
        onClick={onEdit}
        className="pb-2 text-16 font-medium leading-5 text-neutral_60 hover:text-neutral_30"
      >
        편집
      </button>
    </div>
  )
}
```

`src/app/mypage/_components/BlogLinkSection.tsx`:
```tsx
import { SectionShell } from './SectionShell'

export function BlogLinkSection({
  blogUrl,
  onEdit,
}: {
  blogUrl: string | null
  onEdit?: () => void
}) {
  return (
    <SectionShell
      title="내 블로그 연동"
      action={
        <button
          type="button"
          onClick={onEdit}
          className="text-16 font-medium text-neutral_60 hover:text-neutral_30"
        >
          편집
        </button>
      }
    >
      <div className="flex items-center gap-6 rounded-2xl border border-neutral_95 px-6 py-6">
        <span className="size-[62px] shrink-0 rounded-full bg-neutral_95" aria-hidden />
        <div className="flex flex-col gap-1">
          <span className="text-16 font-semibold text-neutral_20">네이버 블로그 URL</span>
          <span className="text-14 text-neutral_60">{blogUrl ?? '연동된 블로그가 없습니다.'}</span>
        </div>
      </div>
    </SectionShell>
  )
}
```

`src/app/mypage/_components/CategorySection.tsx`:
```tsx
import { SectionShell } from './SectionShell'

export function CategorySection({
  categories,
  onEdit,
}: {
  categories: string[]
  onEdit?: () => void
}) {
  return (
    <SectionShell
      title="관심 카테고리"
      action={
        <button
          type="button"
          onClick={onEdit}
          className="text-16 font-medium text-neutral_60 hover:text-neutral_30"
        >
          편집
        </button>
      }
    >
      {categories.length === 0 ? (
        <p className="text-14 text-neutral_60">선택된 관심 카테고리가 없습니다.</p>
      ) : (
        <div className="flex flex-wrap gap-4">
          {categories.map(label => (
            <span
              key={label}
              className="rounded-xl bg-neutral_99 px-4 py-2 text-16 font-medium text-neutral_20"
            >
              {label}
            </span>
          ))}
        </div>
      )}
    </SectionShell>
  )
}
```

`src/app/mypage/_components/AccountNavCard.tsx`:
```tsx
import { ChevronRight } from 'lucide-react'

import { cn } from '@/lib/utils'

export function AccountNavCard({
  title,
  description,
  onClick,
  disabled = false,
  badge,
}: {
  title: string
  description: string
  onClick?: () => void
  disabled?: boolean
  badge?: string
}) {
  return (
    <button
      type="button"
      onClick={disabled ? undefined : onClick}
      aria-disabled={disabled}
      className={cn(
        'flex w-full items-center gap-6 rounded-2xl border border-neutral_95 px-6 py-8 text-left transition-colors',
        disabled ? 'cursor-not-allowed opacity-60' : 'hover:bg-neutral_99'
      )}
    >
      <span className="size-[62px] shrink-0 rounded-full bg-neutral_95" aria-hidden />
      <span className="flex flex-1 flex-col gap-1">
        <span className="flex items-center gap-2">
          <span className="text-18 font-semibold text-neutral_20">{title}</span>
          {badge && (
            <span className="rounded-md bg-neutral_95 px-2 py-0.5 text-12 text-neutral_60">
              {badge}
            </span>
          )}
        </span>
        <span className="text-14 text-neutral_60">{description}</span>
      </span>
      <ChevronRight className="size-6 text-neutral_60" aria-hidden />
    </button>
  )
}
```

- [ ] **Step 4: account/page.tsx 조립 (정적 mock 데이터)**

`src/app/mypage/account/page.tsx`:
```tsx
'use client'

import { AccountNavCard } from '../_components/AccountNavCard'
import { BlogLinkSection } from '../_components/BlogLinkSection'
import { CategorySection } from '../_components/CategorySection'
import { ProfileHeader } from '../_components/ProfileHeader'

export default function AccountPage() {
  return (
    <div className="pb-20">
      <ProfileHeader nickname="사용자 닉네임" />
      <BlogLinkSection blogUrl="https://blog.naver.com/example" />
      <CategorySection categories={['문화', '여행', '테크/IT', '펫']} />

      <section className="mt-16">
        <h2 className="text-24 font-bold text-neutral_20">개인 정보 관리</h2>
        <div className="mt-6 flex flex-col gap-4">
          <AccountNavCard
            title="회원 정보 수정"
            description="이메일, 닉네임 등 내 프로필 정보를 변경할 수 있어요."
          />
          <AccountNavCard
            title="내 결제 수단"
            description="자주 쓰는 결제 수단을 관리하고, 더 빠르고 간편하게 결제해 보세요."
          />
          <AccountNavCard
            title="비밀번호 및 계정"
            description="로그인 비밀번호와 연동 계정을 변경할 수 있어요."
          />
        </div>
      </section>
    </div>
  )
}
```

- [ ] **Step 5: 게이트**

Run: `yarn type-check && yarn lint`
Expected: 에러 0.

- [ ] **Step 6: 런타임 확인 (수동)**

`yarn dev` → `/mypage/account`에서 닉네임/블로그/카테고리/진입카드 3개가 Figma 레이아웃과 유사하게 렌더되는지 확인.

- [ ] **Step 7: 커밋**

```bash
git add src/app/mypage
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -m "feat(mypage): 내 계정 관리 탭 정적 UI"
```

---

### Task 3: "내 계정 관리" 읽기 연동 (getMe → 화면 바인딩)

**Files:**
- Create: `src/app/mypage/hooks/useMyProfile.ts`
- Modify: `src/app/mypage/account/page.tsx`

**Interfaces:**
- Consumes: `userService.getMe()` → `UserProfile` (`@/entities/user`). 필드: `nickname: string | null`, `blogs: Blog[]`(`blogUrl`), `categoryTypes: InterestCategory[]`.
- Produces:
  - `MY_PROFILE_KEY = ['user', 'me']`
  - `useMyProfile()` → `UseQueryResult<UserProfile>`
  - `CATEGORY_LABELS: Record<InterestCategory, string>` (enum→한글 라벨 매핑)

- [ ] **Step 1: useMyProfile 훅 + 카테고리 라벨 맵 작성**

`src/app/mypage/hooks/useMyProfile.ts`:
```ts
'use client'

import { useQuery } from '@tanstack/react-query'

import { userService } from '@/service'

import type { InterestCategory } from '@/entities/user'

export const MY_PROFILE_KEY = ['user', 'me'] as const

export const CATEGORY_LABELS: Record<InterestCategory, string> = {
  FOOD: '맛집',
  BEAUTY: '뷰티',
  CULTURE: '문화',
  TRAVEL: '여행',
  TECH_IT: '테크/IT',
  PET: '펫',
  LIVING: '리빙',
  FASHION: '패션',
  ETC: '기타',
}

export function useMyProfile() {
  return useQuery({
    queryKey: MY_PROFILE_KEY,
    queryFn: () => userService.getMe(),
  })
}
```

- [ ] **Step 2: account/page.tsx를 실데이터로 바인딩 (로딩/에러 처리)**

`src/app/mypage/account/page.tsx` 교체:
```tsx
'use client'

import { AccountNavCard } from '../_components/AccountNavCard'
import { BlogLinkSection } from '../_components/BlogLinkSection'
import { CategorySection } from '../_components/CategorySection'
import { ProfileHeader } from '../_components/ProfileHeader'
import { CATEGORY_LABELS, useMyProfile } from '../hooks/useMyProfile'

export default function AccountPage() {
  const { data: profile, isLoading, isError, refetch } = useMyProfile()

  if (isLoading) {
    return <p className="pt-12 text-16 text-neutral_60">불러오는 중...</p>
  }

  if (isError || !profile) {
    return (
      <div className="flex flex-col items-start gap-3 pt-12">
        <p className="text-16 text-neutral_60">정보를 불러오지 못했습니다.</p>
        <button
          type="button"
          onClick={() => refetch()}
          className="text-16 font-medium text-violet_80"
        >
          다시 시도
        </button>
      </div>
    )
  }

  const blogUrl = profile.blogs[0]?.blogUrl ?? null
  const categoryLabels = profile.categoryTypes.map(c => CATEGORY_LABELS[c])

  return (
    <div className="pb-20">
      <ProfileHeader nickname={profile.nickname ?? '닉네임 미설정'} />
      <BlogLinkSection blogUrl={blogUrl} />
      <CategorySection categories={categoryLabels} />

      <section className="mt-16">
        <h2 className="text-24 font-bold text-neutral_20">개인 정보 관리</h2>
        <div className="mt-6 flex flex-col gap-4">
          <AccountNavCard
            title="회원 정보 수정"
            description="이메일, 닉네임 등 내 프로필 정보를 변경할 수 있어요."
          />
          <AccountNavCard
            title="내 결제 수단"
            description="자주 쓰는 결제 수단을 관리하고, 더 빠르고 간편하게 결제해 보세요."
          />
          <AccountNavCard
            title="비밀번호 및 계정"
            description="로그인 비밀번호와 연동 계정을 변경할 수 있어요."
          />
        </div>
      </section>
    </div>
  )
}
```

- [ ] **Step 3: 게이트**

Run: `yarn type-check && yarn lint`
Expected: 에러 0.

- [ ] **Step 4: 런타임 확인 (수동, 백엔드 필요)**

로컬 백엔드 기동(`docker compose -f ../docker-compose.demo.yml up -d` 또는 기존 방식) + 로그인 후 `/mypage/account` 접근 → 실제 내 닉네임/블로그/카테고리가 표시되는지 확인. 네트워크 탭에서 `GET /api/users/me` 200 확인.

- [ ] **Step 5: 커밋**

```bash
git add src/app/mypage
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -m "feat(mypage): 내 계정 관리 조회 연동(getMe)"
```

---

### Task 4: "내 계정 관리" 쓰기 연동 (닉네임/카테고리/블로그 편집)

**Files:**
- Create: `src/app/mypage/hooks/useProfileMutations.ts`
- Modify: `src/app/mypage/_components/ProfileHeader.tsx` (편집 모드)
- Modify: `src/app/mypage/_components/CategorySection.tsx` (편집 모드)
- Modify: `src/app/mypage/_components/BlogLinkSection.tsx` (편집 모드)
- Modify: `src/app/mypage/account/page.tsx` (편집 상태 연결)

**Interfaces:**
- Consumes: `userService.updateMe(data: UpdateUserProfileRequest)`, `userService.linkBlog({ blogUrl, platform })`, `userService.checkNickname(nickname)` → `{ nickname, available }`. `UpdateUserProfileRequest` 필드: `{ nickname?, categoryTypes?: InterestCategory[], activityTypes?, regionIds? }`.
- Produces: `useUpdateProfile()`, `useLinkBlog()` mutation 훅. 두 훅 모두 onSuccess에서 `MY_PROFILE_KEY` 무효화.

- [ ] **Step 1: ⚠️ 요청 필드명 사전 검증 (백엔드 계약 확인)**

`useMyProfile`이 쓰는 `userService.updateMe`는 `categoryTypes`를 보내고 `http`가 snake로 변환해 `category_types`로 전송한다. 그러나 백엔드 `UserProfileUpdateRequest`는 `interest_categories`를 기대할 수 있다(서버 DTO 확인됨). **구현 전 1회 검증:** 로컬 백엔드에 닉네임만 PATCH해 보고(카테고리 1개 포함) 응답에 카테고리가 반영되는지 확인.
  - 반영되면 그대로 진행.
  - 반영 안 되면(필드명 불일치) `src/service/index.ts`의 `updateMe`만 다음과 같이 매핑 보정:
    ```ts
    updateMe: (data: UpdateUserProfileRequest) =>
      http
        .patch('/api/users/me', {
          ...data,
          ...(data.categoryTypes ? { interestCategories: data.categoryTypes } : {}),
        })
        .then(res => userProfileSchema.parse(res.data)),
    ```
    (`interestCategories` → snake → `interest_categories`.) `categoryTypes` 키는 백엔드가 무시하므로 함께 보내도 무방.

- [ ] **Step 2: mutation 훅 작성**

`src/app/mypage/hooks/useProfileMutations.ts`:
```ts
'use client'

import { useMutation, useQueryClient } from '@tanstack/react-query'

import { userService } from '@/service'

import type { UpdateUserProfileRequest } from '@/entities/user'

import { MY_PROFILE_KEY } from './useMyProfile'

export function useUpdateProfile() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (data: UpdateUserProfileRequest) => userService.updateMe(data),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: MY_PROFILE_KEY }),
  })
}

export function useLinkBlog() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: (data: { blogUrl: string; platform: string }) => userService.linkBlog(data),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: MY_PROFILE_KEY }),
  })
}
```

- [ ] **Step 3: ProfileHeader 편집 모드 (닉네임 + 중복 체크)**

`src/app/mypage/_components/ProfileHeader.tsx` 교체:
```tsx
'use client'

import { useState } from 'react'

import { Button } from '@/components/ui/button'
import { userService } from '@/service'
import { Input } from '@/shared/ui/input/Input'

export function ProfileHeader({
  nickname,
  onSave,
  isSaving = false,
}: {
  nickname: string
  onSave?: (nickname: string) => void
  isSaving?: boolean
}) {
  const [editing, setEditing] = useState(false)
  const [value, setValue] = useState(nickname)
  const [error, setError] = useState<string>()

  const handleSave = async () => {
    const next = value.trim()
    if (!next) {
      setError('닉네임을 입력해 주세요.')
      return
    }
    if (next === nickname) {
      setEditing(false)
      return
    }
    const { available } = await userService.checkNickname(next)
    if (!available) {
      setError('이미 사용 중인 닉네임입니다.')
      return
    }
    setError(undefined)
    onSave?.(next)
    setEditing(false)
  }

  if (!editing) {
    return (
      <div className="flex items-end gap-4 pt-12">
        <h1 className="text-40 font-bold leading-12 text-neutral_20">{nickname}</h1>
        <button
          type="button"
          onClick={() => {
            setValue(nickname)
            setEditing(true)
          }}
          className="pb-2 text-16 font-medium text-neutral_60 hover:text-neutral_30"
        >
          편집
        </button>
      </div>
    )
  }

  return (
    <div className="flex items-end gap-3 pt-12">
      <Input
        label="닉네임"
        value={value}
        onChange={e => setValue(e.target.value)}
        errorMessage={error}
        maxLength={12}
        className="w-80"
      />
      <Button size="md" disabled={isSaving} onClick={handleSave}>
        저장
      </Button>
      <Button size="md" variant="tertiary" onClick={() => setEditing(false)}>
        취소
      </Button>
    </div>
  )
}
```

- [ ] **Step 4: CategorySection 편집 모드 (다중 선택)**

`src/app/mypage/_components/CategorySection.tsx` 교체:
```tsx
'use client'

import { useState } from 'react'

import { Button } from '@/components/ui/button'
import { InterestCategory } from '@/entities/user'

import { cn } from '@/lib/utils'

import { CATEGORY_LABELS } from '../hooks/useMyProfile'
import { SectionShell } from './SectionShell'

const ALL_CATEGORIES = InterestCategory.options

export function CategorySection({
  selected,
  onSave,
  isSaving = false,
}: {
  selected: InterestCategory[]
  onSave?: (next: InterestCategory[]) => void
  isSaving?: boolean
}) {
  const [editing, setEditing] = useState(false)
  const [draft, setDraft] = useState<InterestCategory[]>(selected)

  const toggle = (c: InterestCategory) =>
    setDraft(prev => (prev.includes(c) ? prev.filter(x => x !== c) : [...prev, c]))

  return (
    <SectionShell
      title="관심 카테고리"
      action={
        editing ? (
          <div className="flex gap-2">
            <Button
              size="sm"
              disabled={isSaving}
              onClick={() => {
                onSave?.(draft)
                setEditing(false)
              }}
            >
              저장
            </Button>
            <Button size="sm" variant="tertiary" onClick={() => setEditing(false)}>
              취소
            </Button>
          </div>
        ) : (
          <button
            type="button"
            onClick={() => {
              setDraft(selected)
              setEditing(true)
            }}
            className="text-16 font-medium text-neutral_60 hover:text-neutral_30"
          >
            편집
          </button>
        )
      }
    >
      <div className="flex flex-wrap gap-4">
        {(editing ? ALL_CATEGORIES : selected).map(c => {
          const active = editing ? draft.includes(c) : true
          return (
            <button
              key={c}
              type="button"
              disabled={!editing}
              onClick={() => editing && toggle(c)}
              className={cn(
                'rounded-xl px-4 py-2 text-16 font-medium transition-colors',
                active
                  ? 'bg-neutral_20 text-white'
                  : 'bg-neutral_99 text-neutral_60 hover:bg-neutral_95'
              )}
            >
              {CATEGORY_LABELS[c]}
            </button>
          )
        })}
      </div>
      {!editing && selected.length === 0 && (
        <p className="text-14 text-neutral_60">선택된 관심 카테고리가 없습니다.</p>
      )}
    </SectionShell>
  )
}
```

- [ ] **Step 5: BlogLinkSection 편집 모드 (URL 입력 → linkBlog)**

`src/app/mypage/_components/BlogLinkSection.tsx` 교체:
```tsx
'use client'

import { useState } from 'react'

import { Button } from '@/components/ui/button'
import { Input } from '@/shared/ui/input/Input'

import { SectionShell } from './SectionShell'

export function BlogLinkSection({
  blogUrl,
  onSave,
  isSaving = false,
}: {
  blogUrl: string | null
  onSave?: (blogUrl: string) => void
  isSaving?: boolean
}) {
  const [editing, setEditing] = useState(false)
  const [value, setValue] = useState(blogUrl ?? '')
  const [error, setError] = useState<string>()

  const handleSave = () => {
    const next = value.trim()
    if (!next.startsWith('https://')) {
      setError('https:// 로 시작하는 URL을 입력해 주세요.')
      return
    }
    setError(undefined)
    onSave?.(next)
    setEditing(false)
  }

  return (
    <SectionShell
      title="내 블로그 연동"
      action={
        editing ? null : (
          <button
            type="button"
            onClick={() => {
              setValue(blogUrl ?? '')
              setEditing(true)
            }}
            className="text-16 font-medium text-neutral_60 hover:text-neutral_30"
          >
            편집
          </button>
        )
      }
    >
      {editing ? (
        <div className="flex items-end gap-3">
          <Input
            variant="url"
            label="네이버 블로그 URL"
            value={value}
            onChange={e => setValue(e.target.value)}
            errorMessage={error}
            placeholder="https://blog.naver.com/..."
            className="flex-1"
          />
          <Button disabled={isSaving} onClick={handleSave}>
            저장
          </Button>
          <Button variant="tertiary" onClick={() => setEditing(false)}>
            취소
          </Button>
        </div>
      ) : (
        <div className="flex items-center gap-6 rounded-2xl border border-neutral_95 px-6 py-6">
          <span className="size-[62px] shrink-0 rounded-full bg-neutral_95" aria-hidden />
          <div className="flex flex-col gap-1">
            <span className="text-16 font-semibold text-neutral_20">네이버 블로그 URL</span>
            <span className="text-14 text-neutral_60">
              {blogUrl ?? '연동된 블로그가 없습니다.'}
            </span>
          </div>
        </div>
      )}
    </SectionShell>
  )
}
```

- [ ] **Step 6: account/page.tsx에 편집 핸들러 연결**

`src/app/mypage/account/page.tsx`의 컴포넌트 사용부를 교체(조회 부분은 Task 3 유지):
```tsx
  const updateProfile = useUpdateProfile()
  const linkBlog = useLinkBlog()
  // ...isLoading/isError 분기는 Task 3과 동일...

  return (
    <div className="pb-20">
      <ProfileHeader
        nickname={profile.nickname ?? '닉네임 미설정'}
        isSaving={updateProfile.isPending}
        onSave={nickname => updateProfile.mutate({ nickname })}
      />
      <BlogLinkSection
        blogUrl={blogUrl}
        isSaving={linkBlog.isPending}
        onSave={url => linkBlog.mutate({ blogUrl: url, platform: 'NAVER' })}
      />
      <CategorySection
        selected={profile.categoryTypes}
        isSaving={updateProfile.isPending}
        onSave={categoryTypes => updateProfile.mutate({ categoryTypes })}
      />
      {/* 개인 정보 관리 섹션은 Task 5에서 처리 */}
    </div>
  )
```
상단 import에 추가: `import { useUpdateProfile, useLinkBlog } from '../hooks/useProfileMutations'`. `CategorySection`은 더 이상 라벨 배열이 아닌 `InterestCategory[]`를 받으므로 Task 3에서 만든 `categoryLabels` 변수는 제거.

- [ ] **Step 7: 게이트**

Run: `yarn type-check && yarn lint`
Expected: 에러 0.

- [ ] **Step 8: 런타임 확인 (수동, 백엔드 필요)**

로그인 후 `/mypage/account`에서:
- 닉네임 편집 → 중복 닉네임 입력 시 에러, 정상 닉네임 저장 시 `PATCH /api/users/me` 200 + 화면 갱신.
- 카테고리 편집 → 토글 후 저장 시 반영(Step 1 검증 결과대로 필드명 정확한지 재확인).
- 블로그 URL 편집 → `POST /api/users/me/blog` 200 + 화면 갱신.

- [ ] **Step 9: 커밋**

```bash
git add src/app/mypage
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -m "feat(mypage): 닉네임/카테고리/블로그 편집 연동"
```

---

### Task 5: 개인 정보 관리 카드 — graceful degrade (v1 완료 지점)

**Files:**
- Modify: `src/app/mypage/account/page.tsx`

**Interfaces:**
- Consumes: `AccountNavCard`의 `disabled`, `badge` props (Task 2에서 정의).

- [ ] **Step 1: 진입 카드 3개에 degrade 정책 적용**

`src/app/mypage/account/page.tsx`의 "개인 정보 관리" 섹션을 다음으로 교체:
```tsx
      <section className="mt-16">
        <h2 className="text-24 font-bold text-neutral_20">개인 정보 관리</h2>
        <div className="mt-6 flex flex-col gap-4">
          <AccountNavCard
            title="회원 정보 수정"
            description="닉네임 등 내 프로필 정보를 변경할 수 있어요. (이메일 변경은 준비 중이에요.)"
            badge="일부 준비 중"
            onClick={() => {
              // 닉네임 편집은 상단 ProfileHeader에서 가능. 별도 상세 페이지는 v1 범위 밖.
            }}
          />
          <AccountNavCard
            title="내 결제 수단"
            description="자주 쓰는 결제 수단을 관리하고, 더 빠르고 간편하게 결제해 보세요."
            badge="준비 중"
            disabled
          />
          <AccountNavCard
            title="비밀번호 및 계정"
            description="로그인 비밀번호와 연동 계정을 변경할 수 있어요."
            badge="준비 중"
            disabled
          />
        </div>
      </section>
```

- [ ] **Step 2: 게이트 + 빌드**

Run: `yarn type-check && yarn lint && yarn build`
Expected: 모두 성공.

- [ ] **Step 3: 런타임 확인 (수동)**

`/mypage/account`에서 결제수단·비밀번호 카드가 비활성("준비 중") + 클릭 무반응, 회원정보 수정 카드에 "일부 준비 중" 배지가 보이는지 확인. **여기까지가 v1: 내 계정 관리 탭 end-to-end.**

- [ ] **Step 4: 커밋**

```bash
git add src/app/mypage
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -m "feat(mypage): 개인 정보 관리 카드 graceful degrade (v1 내 계정 관리 완료)"
```

---

### Task 6: 관심 공고 탭

**Files:**
- Create: `src/app/mypage/hooks/useSavedCampaigns.ts`
- Create: `src/app/mypage/_components/SavedCampaignList.tsx`
- Modify: `src/app/mypage/saved/page.tsx`

**Interfaces:**
- Consumes: `myService.getLikes()` → `Campaign[]`, `myService.getRecentViews()` → `Campaign[]` (`@/entities/campaign`의 `campaignSchema`). 카드 렌더는 기존 공용 카드(`src/shared/ui/blog-card` 또는 campaigns 탭의 카드)를 재사용.
- Produces: `useLikes()`, `useRecentViews()` 훅; `SavedCampaignList` (sub-탭: 좋아요 / 최근 본).

- [ ] **Step 1: Figma 디자인 확인**

`get_metadata`로 마이페이지 "관심 공고" 탭 프레임 노드를 찾는다(`702:7858`의 형제 프레임). nodeId 확인 후 `get_design_context`로 레이아웃 토큰 추출.

- [ ] **Step 2: 훅 작성**

`src/app/mypage/hooks/useSavedCampaigns.ts`:
```ts
'use client'

import { useQuery } from '@tanstack/react-query'

import { myService } from '@/service'

export function useLikes() {
  return useQuery({ queryKey: ['my', 'likes'], queryFn: () => myService.getLikes() })
}

export function useRecentViews() {
  return useQuery({ queryKey: ['my', 'recent-views'], queryFn: () => myService.getRecentViews() })
}
```

- [ ] **Step 3: 목록 컴포넌트 + 페이지 조립 (좋아요/최근 본 sub-탭)**

`src/app/mypage/_components/SavedCampaignList.tsx` 및 `saved/page.tsx`에서 두 훅을 sub-탭으로 전환 렌더. 기존 공고 카드 컴포넌트를 재사용하고, 빈 상태("관심 공고가 없습니다.")·로딩·에러 처리. (카드 컴포넌트 경로는 Step 1에서 확인한 기존 컴포넌트를 import.)

- [ ] **Step 4: 게이트 + 런타임 확인**

Run: `yarn type-check && yarn lint`
백엔드 연동: `GET /my/likes`, `GET /my/recent-views` 200 + 목록 표시 확인.

- [ ] **Step 5: 커밋**

```bash
git add src/app/mypage
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -m "feat(mypage): 관심 공고 탭(좋아요/최근 본)"
```

---

### Task 7: AI 히스토리 탭

**Files:**
- Create: `src/app/mypage/hooks/useAiHistory.ts`
- Create: `src/app/mypage/_components/AiHistoryList.tsx`
- Modify: `src/app/mypage/ai-history/page.tsx`

**Interfaces:**
- Consumes: AI 히스토리 조회. 기존 `@/entities/blog-analysis`의 `analysisHistoryResponseSchema`/서비스가 있으면 재사용. 백엔드 엔드포인트 후보: `GET /my/ai-history`(`MyAiHistoryResponse`: `aiHistory[{ historyId, diagnosisDate }]`) 및 `GET /blog/analysis/history`. 상세 진입은 `GET /blog/analysis/{documentId}`.
- Produces: `useAiHistory()` 훅; `AiHistoryList`.

- [ ] **Step 1: 서비스/스키마 확인**

`grep`으로 `analysisHistory`/`/blog/analysis/history`/`/my/ai-history` 관련 기존 서비스 함수와 스키마를 찾는다. 있으면 재사용, 없으면 `myService`에 `getAiHistory` 추가(`GET /my/ai-history`, 응답 스키마는 `entities/blog-analysis` 또는 신규 최소 스키마).

- [ ] **Step 2: 훅 + 목록 컴포넌트 작성**

`useAiHistory.ts`(react-query) + `AiHistoryList.tsx`(날짜·진단 항목 리스트, 항목 클릭 시 기존 분석 상세 라우트로 이동). Figma 토큰은 해당 탭 프레임 `get_design_context`로 반영.

- [ ] **Step 3: 게이트 + 런타임 확인**

Run: `yarn type-check && yarn lint`
백엔드 연동: 히스토리 조회 200 + 목록 표시, 항목 클릭 시 상세 이동 확인.

- [ ] **Step 4: 커밋**

```bash
git add src/app/mypage src/service src/entities
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -m "feat(mypage): AI 히스토리 탭"
```

---

### Task 8: 구독/결제 탭

**Files:**
- Create: `src/app/mypage/hooks/useSubscription.ts`
- Create: `src/app/mypage/_components/SubscriptionSummary.tsx`
- Create: `src/app/mypage/_components/PointsSection.tsx`
- Modify: `src/app/mypage/subscription/page.tsx`

**Interfaces:**
- Consumes: 구독상태는 `useMyProfile`의 `UserProfile`엔 없으므로 로그인 응답/`userService.getMe` 확장 필드 또는 `authAtom.user`(`subscriptionType`,`aiCreditRemaining`)에서 읽는다. 포인트: `myService.getPoints()` → `PointsResponse`(`balance`, `transactions[]`).
- Produces: `usePoints()` 훅; `SubscriptionSummary`(구독상태 read-only), `PointsSection`(잔액 + 거래내역). 결제수단 관리부는 degrade.

- [ ] **Step 1: 구독 상태 출처 확인**

`authAtom`의 `AuthState.user`에 `subscriptionType`/`aiCreditRemaining`이 있는지 `entities/auth/model/auth.schema.ts`에서 확인. 있으면 그 값으로 읽기 전용 표시.

- [ ] **Step 2: usePoints 훅 + 컴포넌트 작성**

`useSubscription.ts`에 `usePoints()`(`queryKey ['my','points']`, `myService.getPoints()`). `SubscriptionSummary`(현재 플랜·AI 크레딧 표시 + "플랜 변경 준비 중" degrade), `PointsSection`(`balance` + `transactions` 리스트, 출금 버튼은 기존 `myService.withdraw` 연결 또는 v1에선 "준비 중" degrade — 디자인 확인 후 결정).

- [ ] **Step 3: 페이지 조립 + degrade**

`subscription/page.tsx`에서 구독요약 + 포인트 섹션 렌더. 결제수단 CRUD·플랜 업/다운그레이드는 "준비 중" 비활성 카드로 노출.

- [ ] **Step 4: 게이트 + 빌드 + 런타임 확인**

Run: `yarn type-check && yarn lint && yarn build`
백엔드 연동: `GET /my/points` 200 + 잔액/내역 표시 확인.

- [ ] **Step 5: 커밋**

```bash
git add src/app/mypage
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -m "feat(mypage): 구독/결제 탭(구독 read-only + 포인트)"
```

---

### Task 9: 마무리 — 상태 처리 점검 + 반응형 + PR

**Files:**
- Modify: 마이페이지 하위 컴포넌트 전반 (필요 시)

- [ ] **Step 1: 공통 상태 점검**

각 탭의 로딩/빈/에러 상태가 일관된지 점검(스피너/스켈레톤, "다시 시도"). 미로그인 가드 동작 재확인.

- [ ] **Step 2: 반응형 점검**

`max-w-300` 컨테이너 기준 데스크탑 레이아웃 확인. (모바일 대응 범위는 디자인 유무에 따라 결정 — 디자인 없으면 데스크탑 우선.)

- [ ] **Step 3: 전체 게이트**

Run: `yarn type-check && yarn lint && yarn build`
Expected: 모두 성공.

- [ ] **Step 4: 최종 커밋 + 푸시 + PR**

```bash
git -c user.name='jwanp' -c user.email='joohwan726@naver.com' commit -am "chore(mypage): 상태 처리/반응형 마무리" || true
git push -u origin feat/mypage
```
PR 생성은 `gh` 계정 `jwanp`로(JECT-Study 푸시 identity). 베이스 `main`, 제목 "feat: 마이페이지 v1 (내 계정 관리 end-to-end + 관심공고/AI히스토리/구독결제)". 본문에 spec/plan 링크와 degrade 항목 명시.

---

## Self-Review

**Spec coverage:**
- 라우팅(중첩) → Task 1 ✓
- 내 계정 관리 4섹션 UI → Task 2 ✓
- getMe 읽기 연동 → Task 3 ✓
- 닉네임(중복체크)/카테고리/블로그 쓰기 연동 → Task 4 ✓
- graceful degrade(결제수단/비밀번호/이메일) → Task 5 ✓
- 관심 공고 / AI 히스토리 / 구독·결제 → Task 6/7/8 ✓
- 상태·에러·반응형·검증 → Task 3~9 ✓
- 로그인 가드 → Task 1 ✓

**Placeholder scan:** Task 6~8의 Figma 노드 ID와 일부 카드 컴포넌트 경로는 "Step 1에서 확인"으로 둠 — 해당 탭은 디자인이 아직 확정 전이고 v1 핵심(Task 1~5)은 완전한 코드를 가짐. v1 범위(Task 1~5)에는 placeholder 없음.

**Type consistency:** `MY_PROFILE_KEY`(Task 3) ↔ mutations(Task 4) 일치. `CategorySection` props가 Task 2(라벨 배열) → Task 4(`InterestCategory[]`)로 변경됨 — Task 4 Step 6에서 `categoryLabels` 제거를 명시해 정합성 유지. `AccountNavCard` props(`disabled`,`badge`)는 Task 2 정의 → Task 5 사용 일치.

**Known risk:** `updateMe`의 `categoryTypes` vs 백엔드 `interest_categories` 필드명 — Task 4 Step 1에서 사전 검증 + 보정 코드 제공.
